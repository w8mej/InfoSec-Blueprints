# TIER 3.1: Generator Testing Framework

**Goal**: Standardized fixture library for testing all 4 generators without boilerplate.

**Files**:

- `tests/fixtures/generator_fixtures.py` (NEW)
- `tests/fixtures/sample_sigma_rules.json` (NEW) — 10 real Sigma rules
- `tests/conftest.py` — Register fixtures

**Fixtures**:

```python
REDACTED
```

**Success**: All generators tested with >= 80% coverage using fixtures.
